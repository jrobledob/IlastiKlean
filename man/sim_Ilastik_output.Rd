% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_Ilastik_output.R
\name{sim_Ilastik_output}
\alias{sim_Ilastik_output}
\title{Generate and save synthetic plant datasets}
\usage{
sim_Ilastik_output(samples, pictures, wor_dir)
}
\arguments{
\item{samples}{Integer. Number of samples per plant.}

\item{pictures}{Integer. Number of pictures per sample.}

\item{wor_dir}{Character. Path to directory where CSV files will be written.}
}
\value{
A named list of \code{data.frame}s, one element per plant/sample/picture,
named like \code{dataset.ch00_plant1_sample1_pic1}.
}
\description{
This function simulates measurement data for two plants, each with a given
number of samples and pictures per sample.  For plant 1 it draws classes from
\code{c("Callose", "Artifact", "Background")} and for plant 2 from
\code{c("Callose", "Callose", "Background", "Callose")}.  All datasets are saved
as CSV files in \code{wor_dir} and returned as a named list.
}
\examples{
\dontrun{
## Create 3 samples × 3 pictures and save CSVs in "./out"
ds <- generate_datasets(samples = 3,
                        pictures = 3,
                        wor_dir = "./out")
}
}
