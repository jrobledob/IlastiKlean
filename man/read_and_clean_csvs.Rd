% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_and_clean_csvs.R
\name{read_and_clean_csvs}
\alias{read_and_clean_csvs}
\title{Read and combine CSV files with cleaned column names}
\usage{
read_and_clean_csvs(path = ".", pattern = "\\\\.csv$")
}
\arguments{
\item{path}{Character. Directory containing the CSV files. Defaults to \code{"."}.}

\item{pattern}{Character. A regular expression to match files. Defaults to \code{"\\\\.csv$"}.}
}
\value{
A tibble with all rows from every CSV, all columns as character,
with an extra \code{filename} column (base name only) and clean column names.
}
\description{
Reads all CSV files in the directory given by \code{path}, coerces every column
to character, binds them into one tibble, adds a \code{filename} column (just the
file name, no path), and replaces spaces in column names with underscores.
}
\examples{
\dontrun{
# CSVs in the working directory
df1 <- read_and_clean_csvs(path = "./")

# CSVs in some other folder
df2 <- read_and_clean_csvs(path = "data/")
}
}
